1. mkdir ~/Projects
1. cd ~/Projects
1. Clone the git repo
    1. `git clone git@github.com:symless/synergy-core.git`
1. Switch to the right branch
    1. Beta v2.0: `git checkout master`
    1. Stable v1.10: `git checkout v1.10`